#!/bin/sh
# ===========================================
# GLOBAL SETTINGS
# ===========================================

# Set country code if needed (optional - no default is set)
# Uncomment and set to your country code if required
# COUNTRY_CODE="FI"

# ===========================================
# HARDWARE-SPECIFIC ADJUSTMENTS
# ===========================================

HARDWARE_MODEL=$(cat /tmp/sysinfo/model 2>/dev/null || echo "unknown")

case "$HARDWARE_MODEL" in
    *"Archer"*)
        # Archer routers - known to run warm, reduce power further
        # JUST AN EXAMPLE, NOT NECESSARILY RECOMMENDED CONFIGURATION
        RADIO_OVERRIDE_2g_txpower="13"
        RADIO_OVERRIDE_5g_txpower="16"
        ;;
    *"Netgear"*)
        # Netgear routers - usually good thermal management
        # JUST AN EXAMPLE, NOT NECESSARILY RECOMMENDED CONFIGURATION
        RADIO_OVERRIDE_2g_txpower="15"
        RADIO_OVERRIDE_5g_txpower="18"
        ;;
esac
